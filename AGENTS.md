# 品牌策划全案 SOP 项目规则

> 本项目特定规则，优先级高于全局 AGENTS.md。  
> 通用规则请参考：~/.config/opencode/AGENTS.md

---

## 一、项目定义

本项目为品牌策划全案 SOP 构建项目，目标是将品牌策划流程沉淀为可迭代、可复用、可版本管理的结构化方法论体系。

项目采用：
- 模块化结构
- Git / GitHub 历史保留
- 单主文件工作区模式

---

## 二、术语定义

1. **章节**：指一个独立的 SOP 主文件，对应一个完整章节编号（如 1.1、1.2、2.3）。

2. **模块**：指该 SOP 主文件中的一级正式章节；除非另有说明，不包括其内部的二级、三级及以下子标题。

3. **当前主文件**：指当前会话中用户明确指定的目标文件。当前允许范围包括：
   - `.sop/config/` 下的文件
   - `交付文档/` 下的文件
   - `思考逻辑梳理/` 下的文件

4. 如当前会话中存在多个候选文件，或用户未明确指定目标文件，必须先澄清后再继续，不得自行推断。

5. **阶段**：指用户明确声明的一个工作里程碑，不默认等同于章节完成。

6. **交付完成**：指某一目标文件发生了需要对外保存、归档或进入版本管理的有效内容修改，并经用户确认结束本轮交付。

7. **恢复旧版本**：指将目标历史版本的完整正文内容恢复为当前文件内容基线，并在“版本历史”章节追加新的回溯记录。除非用户明确要求局部回滚，否则不得只恢复部分内容。

---

## 三、项目内规则优先级

项目内规则冲突时，按以下优先级解释：

1. 异常边界处理高于常规流程。
2. 通用确认规则高于任何文件修改、commit、push 执行条款。
3. 结构与模板定义高于示例文本。
4. 若仍无法消解冲突，必须暂停并请求用户确认。

---

## 四、结构规则（Structure Rules）

1. 采用扁平化交付结构，所有 SOP 主文件统一存放在 `交付文档/` 目录，不创建模块子目录。

2. SOP 主文件命名必须遵循统一规范：

   ```text
   {章节号}-{章节名称}SOP.md
   ```

   示例：
   - `1.1-行业研究SOP.md`
   - `1.3-消费者研究SOP.md`

3. 同一章节在工作区中只保留 1 个正式主文件，不保留带版本后缀的并行文件。

4. 版本演进信息必须内嵌在 SOP 文件附录中管理，不创建独立 README 文件，不创建 v1 / v2 / v3 并行文件。

---

## 五、通用确认规则

1. 所有涉及文件修改的操作，必须先向用户说明修改范围。

2. 对小范围、低风险、对象明确的修改，可在说明修改范围后直接执行，无需单独输出详细计划。

3. 对以下非平凡修改，必须先输出变更计划，并等待用户明确确认：
   - 多文件修改
   - 结构调整
   - 版本恢复
   - 规则重写
   - 会触发 commit / push 的修改
   - 其他影响范围不明确的修改

4. 未获用户明确确认前，不得执行非平凡修改。

5. 仅当用户明确回复“确认”或“使用默认值”等可执行指令时，方可继续后续修改流程。

6. 本章为所有文件修改、恢复、提交、推送动作的前置门槛；后文所有“必须执行”的规则，仅在本章条件满足后生效。

---

## 六、版本管理规则（Versioning Rules）

1. 历史版本通过 Git / GitHub 提交记录和文档附录中的“版本历史”共同保留。

2. 每次对 `交付文档/` 下 SOP 主文件做出有效内容修改后，必须执行 Git commit 并 push 到 GitHub。

3. **有效内容修改**，指对 SOP 主体业务内容产生实质影响的变更，包括：
   - 新增、删除或修改主体业务内容及其正文要求
   - 调整分析框架或方法论
   - 更新数据获取路径、来源、参数或引用方式
   - 修改输出格式、判断标准或质量检查标准

4. 下列操作单独发生时，不视为有效内容修改，不触发 commit + push：
   - 格式调整，例如缩进、空行、标点
   - 错别字修正
   - 注释补充
   - 仅新增版本历史记录（但恢复流程触发的版本历史更新除外）

5. 如“版本历史”更新属于恢复流程或优化流程的一部分，且同次操作中同时包含有效内容修改，则必须随该次有效内容修改一并 commit + push。

6. 恢复规则属于独立触发条件：使用 Git / GitHub 历史恢复旧版本后，无论是否构成“有效内容修改”，都必须在当前文件“版本历史”章节新增一条记录，并执行 Git commit 与 push。

7. 版本历史记录必须写明：
   - 版本标识
   - 恢复或优化原因
   - 关键变更点

---

## 七、典型场景执行规则

### 7.1 当用户说“优化当前 SOP”

系统必须执行：
- 先确认当前主文件是否已唯一锁定
- 澄清优化目标
- 澄清优化范围
- 对非平凡修改输出优化方案或修改计划
- 等待用户确认
- 仅在确认后修改目标文件
- 修改后更新“版本历史”章节
- 若构成有效内容修改，则执行 Git commit 与 push

### 7.2 当用户说“回到某个版本（如 v1 / 行业研究初版）”

系统必须执行：
- 优先依据目标文件“版本历史”中的明确版本标识定位目标版本
- 若用户使用自然语言别名，必须先映射到唯一版本标识
- 若无法唯一映射，必须暂停并澄清
- 恢复目标文件的完整正文内容
- 在当前文件“版本历史”章节新增回溯记录，并标注“回溯使用版本”
- 执行 Git commit 与 push

### 7.3 当用户说“完成一个阶段 / 保存当前版本”

系统必须执行：
- 检查当前主文件是否存在有效内容修改

如存在有效内容修改：
- 补写版本历史章节
- 执行 Git commit 与 push

如不存在有效内容修改：
- 仅允许补充版本历史记录
- 不得虚构内容变更
- 且该补充单独发生时不触发 commit + push

### 7.4 当用户请求修改规则文本本身时

系统必须执行：
- 先说明拟修改范围
- 对非平凡修改输出变更计划
- 等待用户确认后再编辑
- 未确认前不得直接改写规则文件

---

## 八、GitHub 同步规则

1. 所有版本操作的目标状态为：
   - 本地 commit 成功
   - 远端 push 成功

2. commit message 必须包含：
   - 章节编号与章节名称
   - 变更摘要

3. 示例：

   ```text
   feat: 1.1 行业研究（竞品分析增强 + 流程优化）
   ```

4. 版本号可选，仅在需要强调版本演进时添加。

5. 执行 Git commit 时，默认仅提交本次目标文件及其必须同步更新的关联文件；未经用户确认，不得将其他无关改动一并纳入同一提交。

6. 如 push 失败，本次版本操作视为“部分完成”：
   - 必须保留本地 commit
   - 必须明确告知用户远端尚未同步
   - 在用户确认前不得自动重试 push

---

## 九、SOP v3.0 执行规则

### 9.1 数据获取
- 所有 SOP 主文件的数据获取必须通过 `.sop/config/data-acquisition.yaml` 配置。
- 默认数据源示例为 Exa / Tavily；实际生效的数据源必须以配置文件中的 `primary` 字段为准。
- 数据调用使用自然语言查询模板，不使用固定搜索词。

### 9.2 数据融合
- 外部数据与内部数据是“融合验证”关系，不是“覆盖替代”关系。
- 详见 `交付文档/SOP-融合与冲突处理规则.md`。

### 9.3 内部文件处理
1. 当内部输入对象以项目文件形式存在时，允许使用简洁文件名的前提是：
   - 基于当前会话中的明确信息或工具检索结果，仅存在 1 个候选文件
   - 后续操作不需要工具调用、代理协助、审计留痕或直接文件操作

2. “唯一识别到 1 个匹配文件”是指：基于当前会话中的明确信息或工具检索结果，仅存在 1 个候选文件。若未检索且不能显式排除其他候选文件，不得视为唯一识别。

3. 只要存在以下任一情况，必须改用正式项目文件引用：
   - 存在多个候选文件，需要消歧
   - 下一步需要读取、编辑、查证、交由工具或代理执行
   - 需要形成可审计、可复核的记录

4. 正式项目文件引用默认使用 canonical 路径：

   ```text
   ~/github/market-research-sop/...
   ```

5. `.sop/config/internal-files.yaml` 仅作为内部文件背景映射来源；其映射结果不得覆盖本节的引用精度与触发条件。

### 9.4 模板使用
- 所有 SOP 主文件必须基于 `交付文档/SOP-通用模板.md` 的骨架结构创建。
- 模板仅定义模块骨架，具体内容由各模块自定义。

### 9.5 强制交互规则
- 所有 SOP 主文件中的参数确认步骤，遵循第五章通用确认规则执行。

---

## 十、版本历史内嵌规范

1. 每个 SOP 文件必须在附录部分包含“版本历史”章节；如模板演进导致章序变化，以模板定义为准，不强制固定为第八章。

2. 版本历史用于记录版本演进逻辑，不得独立拆出为单独文件。

3. 版本历史标准结构示例如下：

   ```markdown
   ## 版本历史

   ### v1（初版）
   初版流程，覆盖基础行业分析逻辑

   ### v2（优化版）
   优化点：
   - 增加竞品分析维度
   - 调整信息收集路径
   - 明确输出格式

   ### v3（结构重构版）
   优化点：
   - 重构分析框架
   - 提升模块复用性
   ```

4. 示例仅示意内容结构；实际插入时必须遵循当前文件的标题层级，不得机械复制为文档顶级标题。

---

## 十一、SOP 前置声明规范

1. 前置声明仅适用于每个 SOP 主文件中的一级正式模块，不适用于二级、三级及以下子标题。

2. 前置声明仅用于说明该模块的内容范围和所需输入类型，不展开方法细节；除非确有必要，不应超过 2–3 行。

3. 前置声明中的输入项必须按该模块所属章节编号对应的输入类型分别书写：
   - 数据优先章节：使用 `> **所需数据**：...`
   - 资料优先章节：使用 `> **所需资料**：...`
   - 数据 + 资料双输入章节：必须分两行分别写：
     - `> **所需数据**：...`
     - `> **所需资料**：...`

4. 不得将双输入章节合并写成“所需数据/资料”。

5. “数据”与“资料”的定义如下：
   - 数据：用于可量化、可验证的信息
   - 资料：用于文档、案例、规范、资产材料

---

## 十二、章节输入分类规范

1. 按章节编号定义输入类型，分为三类：
   - 数据优先
   - 资料优先
   - 数据 + 资料双输入

2. 分类依据必须使用完整章节编号映射，不使用抽象标签或模糊分类。

### 12.1 数据优先
- 1.1 行业研究
- 1.2 竞争分析
- 1.3 消费者研究
- 1.4 趋势分析
- 1.5 机会分析
- 2.2 目标市场
- 4.1 产品线规划
- 4.2 定价策略
- 5.1 渠道策略
- 5.4 促销与活动
- 5.5 经销商体系
- 5.6 用户运营 / CRM
- 5.7 数字化营销
- 6.1 供应链规划
- 6.2 生产质量保障
- 6.3 成本结构
- 6.4 仓储物流
- 6.5 供应商管理
- 6.6 可持续供应链
- 6.7 现金流预测（运营可行性）
- 7.2 效果监控
- 7.3 复盘优化
- 7.4 里程碑验收
- 7.5 综合财务可行性分析（整合三阶段成果）

### 12.2 资料优先
- 2.4 品牌愿景使命
- 2.5 初步财务预测（方向可行性）
- 3.1 品牌命名
- 3.2 视觉识别
- 3.3 品牌调性
- 3.4 品牌故事
- 4.3 包装设计
- 4.5 产品故事
- 4.6 详细财务模型（产品可行性）
- 8.1 商标与知识产权
- 8.2 食品安全 / 生产资质
- 8.3 危机公关预案
- 8.4 数据合规
- 8.5 合同与法务

### 12.3 数据 + 资料双输入
- 2.1 品牌定位
- 2.3 差异化策略
- 4.4 产品卖点提炼
- 5.2 传播策略
- 5.3 内容策略
- 7.1 上市计划

4. 双输入章节处理规则：
   - 必须在该章节对应 SOP 主文件的相关模块前置声明中，同时标注所需数据和所需资料
   - 数据获取和资料引用必须分别明确来源
   - 数据与资料融合时必须标注融合逻辑

5. 对于双输入章节，在开始该章节对应 SOP 主文件的正式产出前，必须确认该章节整体至少具备 1 类可用数据来源和 1 类可用资料来源。若任一来源缺失，必须暂停并提示用户补充，不得使用默认值或假设值。

---

## 十三、思考逻辑梳理规范

1. 每次章节主文件交付完成后，必须同步创建或更新该章节对应的思考逻辑梳理文档。

2. 同一章节只维护 1 个固定的思考逻辑梳理文件；首次随该章节主文件交付时创建，后续随同一章节主文件交付时更新，不得为同一章节创建多个并行文件。

3. 存放路径：

   ```text
   思考逻辑梳理/{章节号}-{章节英文名}-thinking-logic.md
   ```

4. 文档结构必须包含以下五部分：
   - 决策演进树
   - 设计原则演进
   - 关键决策点
   - 自我纠错记录
   - 版本演进总结

---

## 十四、异常边界处理

1. **Push 失败处理**
   - 必须保留本地 commit
   - 提示用户检查网络连接、GitHub 权限或分支冲突
   - 由用户确认后，方可重试 push

2. **工作区无关改动处理**
   - 本条所称“工作区无关改动”，不包括本规则要求同步生成或更新的配套文件
   - 当前允许的 SOP 相关改动范围包括：
     - `交付文档/`
     - `思考逻辑梳理/`
   - 除此之外的改动，才视为工作区无关改动
   - 检测到工作区无关改动时，必须先暂停 SOP 相关操作
   - 提示用户先 stash 或提交无关改动
   - 用户确认后再继续
   - 恢复 stash 仅在用户确认且操作安全时执行

3. **双输入缺失处理**
   - 检测到双输入章节缺少任一必需来源时，必须暂停
   - 明确提示用户补充缺失来源
   - 不得使用默认值或假设值

4. **用户拒绝确认处理**
   - 适用于所有受第五章通用确认规则约束的操作
   - 用户拒绝确认时，不执行任何修改、恢复、提交或推送操作
   - 保留当前状态并结束本次流程

5. **版本历史冲突处理**
   - 恢复旧版本时，如发现版本历史记录缺少以下任一必填项，必须暂停：
     - 版本标识
     - 恢复或优化原因
     - 关键变更点
   - 仅格式差异或表述长短不一，不单独视为“不完整”
   - 基于现有信息推断描述性版本语义时，必须标注“推断”
   - 不得对恢复目标本身进行推断；若目标版本无法唯一确定，必须先暂停并向用户澄清

---

## 十五、一句话总结

本系统是一个：基于 GitHub 的品牌策划方法论版本管理系统（Versioned SOP System）
