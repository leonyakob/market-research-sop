# Task 5 证据：模块输入分类嵌入 AGENTS.md 候选文本

## 结论
- 状态：PASS
- 结果：已将模块输入分类嵌入候选 AGENTS 文本，且三类分类可由标题与清单直接核对。

## 依据
- 任务 3 映射文件给出了完整分类基线；候选文本按三类重新分配后，仍覆盖全部 43 个模块编号。
- SOP 框架文件确认模块总表与编号一致，子模块共 43 个，参考 `## 一` 到 `## 八`。

## 核对点
- 已使用完整模块编号，不存在“某某类模块”这类模糊标签。
- 已保留 task 2 的长期规则，包括：
  - 版本历史内嵌规范
  - GitHub 同步规则
  - SOP v3.0 执行规则
  - 章节前置声明规范
  - 思考逻辑梳理规范
- 候选文本中新增了 `## 十一、模块输入分类规范`，并按三类明确展开，其中 `11.3` 仅包含 2.1、2.3、4.4、5.2、5.3、7.1。

## 参考位置
- `.sisyphus/drafts/task-3-module-input-mapping.md`（分类基线）
- `交付文档/1-品牌策划全案SOP框架.md`（模块总表，见 `## 一`-`## 八`）
- `.sisyphus/drafts/task-5-agents-candidate.md`（`## 十一、模块输入分类规范`）
