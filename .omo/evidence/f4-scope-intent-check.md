# F4 范围与用户要求符合性复核

## 复核对象
- `.sisyphus/drafts/agents-md-longterm-rules.md`
- `.sisyphus/drafts/final-agents-candidate.md`
- `.sisyphus/plans/update-agents-md-longterm-rules.md`

## 结论摘要
- Scope: PASS
- User Intent: PASS
- Final Verdict: APPROVE

## 事实核对

### 1. 单主文件工作区策略
- 已确认：工作区只保留一个当前主文件，不保留 v1/v2/v3 多文件（draft 5 行；计划 22, 26, 53, 73, 653 行）。
- 候选稿已落地：项目采用“单主文件工作区模式”，且同一模块只保留一个当前主文件，不保留版本后缀文件（候选稿 12, 36 行）。

### 2. 文件名不带版本号后缀
- 已确认：SOP 主文件文件名不带版本号后缀（draft 6 行；计划 22, 27, 74 行）。
- 候选稿命名规则为 `{模块号}-{模块名称}SOP.md`，示例均无版本号后缀（候选稿 20-29 行）。

### 3. Git / GitHub 历史保留
- 已确认：历史版本通过 Git / GitHub 提交记录 + 文档附录中的“版本历史”保留（draft 7-9 行；计划 22, 29-30, 53, 75-77, 654 行）。
- 候选稿已落地：历史通过 Git / GitHub 提交记录与版本历史保留；有效内容修改后必须 commit + push；恢复旧版本后必须补写版本历史并 commit + push（候选稿 38-58, 136-153 行）。

### 4. 优化前必须先确认
- 已确认：当用户要求“优化当前 SOP”时，不能立即修改，必须先澄清目标和范围、输出修改计划、等待确认（draft 10, 14-15 行；计划 31, 64, 78, 89, 455-469, 655 行）。
- 候选稿已落地：第五章增加通用确认规则，要求先说明修改范围或输出变更计划，并在用户明确确认前停止生成、不得直接修改；“优化当前 SOP”规则也保持先澄清、先计划、后修改（候选稿 90-106 行）。

### 5. 模块编号归类 + 双输入精确集合
- 已确认：规则必须按模块编号归类，并允许双输入模块；必须提供完整映射表（draft 13-15 行；计划 57, 65-68, 79-81, 656 行）。
- 候选稿已落地：第十一章使用完整模块编号映射，三分类齐全；双输入模块精确集合为 2.1、2.3、4.4、5.2、5.3、7.1（候选稿 227-292 行，尤其 280-287 行）。

### 6. 计划阶段不直接修改根目录 `AGENTS.md`
- 已确认：当前先生成计划，经确认后，再派其他 agent 修改 `AGENTS.md`（draft 4, 14 行；计划 22, 70, 84, 157, 571, 604-610, 639-640, 658 行）。
- 候选稿已落地：第十四章明确说明该节仅适用于候选稿评审与交接阶段；当前文件为最终候选草案；仅当用户明确确认采纳后，方可由其他 agent 编辑根目录 `AGENTS.md`（候选稿 332-335 行）。

## 范围复核

### 范围内内容
- `AGENTS.md` 长期策略更新
- Git / GitHub 历史保留规则
- SOP 修改 / 回溯 / 保存行为规则
- 模块输入分类映射
- 新路径引用统一

以上均与计划定义的核心目标、交付物和 F4 复核范围一致（计划 5-11, 52-59, 631-633 行）。

### 未发现越界扩展
- 未扩展成批量重写全部 SOP 文件。
- 未引入新自动化脚本、CI、hook、配置架构或自动化系统改造。
- 未在计划阶段直接修改根目录 `AGENTS.md`。

这些限制在 draft 与 plan 中均被显式保留，候选稿未突破该边界（draft 31-34 行；计划 43-46, 83-90, 212-215, 249-255, 497-505, 631-633 行；候选稿 332-335 行）。

## 风险点检查
- 候选稿新增了更严格的“通用确认规则”和“版本历史仅单独发生时不触发 commit + push”等表述，但这些仍属于已确认的 AGENTS.md 长期治理规则细化，不构成自动化系统改造。
- 候选稿未出现“直接批量重写现有 SOP 文件”“自动生成全部 SOP”“重构自动化体系”等超范围内容。

## 复核结论
候选稿继续严格匹配已确认的用户意图，并保持在计划限定范围内：仅覆盖 `AGENTS.md` 长期策略、行为规则、分类映射、Git/GitHub 规则与路径修正；未扩展为 SOP 全量重写或自动化改造。

`Scope PASS | User Intent PASS | VERDICT: APPROVE`
